# Backend environment example for backend_triagens

# The host/port the Express app listens on
HOST=0.0.0.0
PORT=3000

# MongoDB connection (provided by database_triagens)
MONGODB_URL=mongodb://user:pass@localhost:27017
MONGODB_DB=triage_db

# Auth secret for JWT (used by auth scaffolding)
JWT_SECRET=replace-with-strong-secret
JWT_EXPIRES_IN=1h

# NODE_ENV can be development|production|test
NODE_ENV=development
